import React from "react";
import Invoice from "@/components/admin/invoice/invoice";

export const metadata = {
  title: "Invoice",
  description: "created for invoice admin",
};

const LoginPage = () => {
  return <Invoice />;
};

export default LoginPage;
