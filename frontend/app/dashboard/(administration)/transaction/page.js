import React from "react";

import Transaction from "@/components/admin/transaction/Transaction";

export const metadata = {
  title: "Transactions",
  description: "created for transaction admin",
};

const OrderPage = () => {
  return <Transaction />;
};

export default OrderPage;
