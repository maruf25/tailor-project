import React from "react";

import OrderComponent from "@/components/admin/order/order";

export const metadata = {
  title: "Order",
  description: "created for order admin",
};

const OrderPage = () => {
  return <OrderComponent />;
};

export default OrderPage;
